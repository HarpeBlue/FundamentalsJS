var nombre = 'Harold', edad = 24;

// Pedazos de código reutilizable.

function imprimirEdad(name, years) {
  console.log(`${name} tiene ${years} años`);
}

imprimirEdad(nombre, edad);

// Lenguaje débilmente tipado.